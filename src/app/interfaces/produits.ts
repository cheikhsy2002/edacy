export interface Simpleproduct{
id: string;
name: string;
description: string;
image: string;
price?: string;
categorie?: string;
promo: boolean;
promoval: number;

// propriétés qui finisse avec ? est optionnel
}